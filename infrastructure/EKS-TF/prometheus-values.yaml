# Ref - https://github.com/prometheus-community/helm-charts/blob/main/charts/kube-prometheus-stack/values.yaml
# -- Create default rules for monitoring the cluster
defaultRules:
  create: true

prometheus:
  enabled: true

# -- Grafana 
grafana:
  enabled: true
  adminUser: thanhloc
  adminPassword: admin123
  service:
    type: ClusterIP
  datasources:
    datasources.yaml:
      apiVersion: 1
      datasources:
        - name: prometheus
          type: prometheus
          access: proxy
          url: http://prometheus-stack-kube-prom-prometheus.monitoring:9090

  ingress:
    enabled: true
    ingressClassName: alb
    tls: false
    hosts:
      - grafana.netsena.io.vn
    path: /
    annotations:
      kubernetes.io/ingress.class: alb
      alb.ingress.kubernetes.io/scheme: internet-facing
      alb.ingress.kubernetes.io/target-type: ip
      alb.ingress.kubernetes.io/backend-protocol: HTTP
      alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80}]'
      alb.ingress.kubernetes.io/load-balancer-name: "alb-grafana"
      alb.ingress.kubernetes.io/group.name: "grafana-alb-group"
    
  persistence:
    enabled: true
    type: pvc
    storageClassName: "longhorn"
    accessModes:
      - ReadWriteOnce
    size: 10Gi
    finalizers:
      - kubernetes.io/pvc-protection
  
# -- Alertmanager 
# alertmanager:
#   enabled: true
#   service:
#     type: ClusterIP
#   ingress:
#     enabled: true
#     ingressClassName: alb
#     hosts:
#       - alertmanager.netsena.io.vn
#     paths:
#       - /*
#     annotations:
#       kubernetes.io/ingress.class: alb
#       alb.ingress.kubernetes.io/scheme: internet-facing
#       alb.ingress.kubernetes.io/target-type: ip
#       alb.ingress.kubernetes.io/backend-protocol: HTTP
#       alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80}]'
#       alb.ingress.kubernetes.io/load-balancer-name: "alb-alertmanager"
#       alb.ingress.kubernetes.io/group.name: "alertmanager-alb-group"

# -- RBAC (tùy chọn, thường nên bật)
rbacEnable: true

# -- Node exporter
nodeExporter:
  enabled: true

# -- Kube-state-metrics
kubeStateMetrics:
  enabled: true
