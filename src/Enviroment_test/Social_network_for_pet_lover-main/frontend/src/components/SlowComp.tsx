import React from 'react'


// const wait = (ms) => {
//     const start = Date.now;
//     let now = start
//     while(now -start <ms) now = Date.now
// }
const SlowComp = () => {
  return (
    <div>
      
    </div>
  )
}

export default SlowComp
